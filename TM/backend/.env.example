# MongoDB Atlas Connection String
# Get this from MongoDB Atlas: Database > Connect > Connect your application
# Format: mongodb+srv://<username>:<password>@<cluster-url>/<database-name>?retryWrites=true&w=majority
mongoDbURI=mongodb+srv://username:password@cluster.mongodb.net/taskmanager?retryWrites=true&w=majority

# Server Port
PORT=1200

# JWT Secret Key - Use a strong random string in production
# Generate one using: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
JWT_SECRET=your_jwt_secret_key_here
